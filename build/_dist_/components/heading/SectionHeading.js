import './SectionHeading.css.proxy.js';
/* src\components\heading\SectionHeading.svelte generated by Svelte v3.31.2 */
import {
	SvelteComponent,
	append,
	attr,
	detach,
	element,
	init,
	insert,
	noop,
	safe_not_equal,
	set_data,
	text
} from "../../../web_modules/svelte/internal.js";

function create_fragment(ctx) {
	let section;
	let h2;
	let t;

	return {
		c() {
			section = element("section");
			h2 = element("h2");
			t = text(/*title*/ ctx[0]);
			attr(h2, "class", "section-heading__title svelte-1rndo73");
			attr(section, "class", "section-heading svelte-1rndo73");
		},
		m(target, anchor) {
			insert(target, section, anchor);
			append(section, h2);
			append(h2, t);
		},
		p(ctx, [dirty]) {
			if (dirty & /*title*/ 1) set_data(t, /*title*/ ctx[0]);
		},
		i: noop,
		o: noop,
		d(detaching) {
			if (detaching) detach(section);
		}
	};
}

function instance($$self, $$props, $$invalidate) {
	let { title = "" } = $$props;

	$$self.$$set = $$props => {
		if ("title" in $$props) $$invalidate(0, title = $$props.title);
	};

	return [title];
}

class SectionHeading extends SvelteComponent {
	constructor(options) {
		super();
		init(this, options, instance, create_fragment, safe_not_equal, { title: 0 });
	}
}

export default SectionHeading;